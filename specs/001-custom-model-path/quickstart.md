# Quickstart Walkthrough

1. Navigate to the **Settings** tab in the application.
2. Locate the **"Model Storage Location"** section.
3. Click "Change Location" and select a drive or folder using the native system dialog.
4. Proceed to download an embedding model on the First Run screen or continue app usage; models will now be downloaded to and read from the chosen directory, bypassing the system default cache.
